
spring:
  cloud:
    config:
      name: filink-station
      label: filink-station
      profile: dev
      discovery:
        enabled: true
        service-id: FILINK-CONFIG-SERVER

eureka:
  client:
    service-url:
      defaultZone: http://filink:123456@10.5.24.224:8761/eureka/
  instance:
    prefer-ip-address: true

