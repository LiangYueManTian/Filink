
spring:
  cloud:
    config:
      name: filink-rfid
      label: filink-rfid
      profile: dev
      discovery:
        enabled: true
        service-id: FILINK-CONFIG-SERVER

eureka:
  client:
    service-url:
      defaultZone: http://10.5.24.224:8761/eureka/
  instance:
    prefer-ip-address: true

