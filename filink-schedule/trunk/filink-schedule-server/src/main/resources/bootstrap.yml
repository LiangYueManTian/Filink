
spring:
  cloud:
    config:
      name: filink-schedule
      label: filink-schedule
      profile: dev
      discovery:
        enabled: true
        service-id: FILINK-CONFIG-SERVER

eureka:
  client:
    service-url:
      defaultZone: http://10.5.24.141:8762/eureka/
  instance:
    prefer-ip-address: true

