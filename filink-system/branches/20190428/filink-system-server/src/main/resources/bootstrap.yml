
spring:
  cloud:
    config:
      name: filink-system
      label: filink-system
      profile: dev
      discovery:
        enabled: true
        service-id: FILINK-CONFIG-SERVER

eureka:
  client:
    service-url:
      defaultZone: http://10.5.24.224:8761/eureka/
  instance:
    prefer-ip-address: true
#spring:
#  profiles:
#    active: dev

